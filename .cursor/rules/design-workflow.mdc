---
description: JD AI 对话导购助手设计稿生成规范（v4 框架驱动）。用户描述商品推荐/意图收集场景、或要生成回复稿时触发。
globs:
  - "design/**/*.md"
  - "render/**/*.js"
  - "schemes/**"
alwaysApply: true
---

# 设计稿生成 · 工作流入口

⚠ **本规范的权威源是仓库根目录的 [AGENTS.md](../../AGENTS.md)**，请先读它。本文件只是 Cursor 的触发指针，不重复内容（避免漂移）。

## 一句话工作流

1. 照 [design/content-schema.md](../../design/content-schema.md) 把场景写成 schema 对象（顶层 `framework`：`sections_h` / `grid_v` / `r_grid` / `intent_collect`）。
2. **读 [render/render-scheme.js](../../render/render-scheme.js) 全文**（禁止凭记忆重建），粘到 `use_design_script` 脚本头部，末尾 `return await renderScheme(SCHEME)`。
3. 看 `selfCheck` / `warnings`，再 `get_screenshot` 截图。

## 硬规则（详见 AGENTS.md）

- 布局不手写：不写 `createAutoLayout` / 节点骨架 / 间距 / 颜色，全走渲染器。
- 生成前必读 render-scheme.js 全文。
- 需接入 Relay / zero-design MCP 才能落地渲染；没有则只产出 `scheme.json`。
- 默认 Relay：fileKey `2059170750769684481` · pageId `0:2`。

> ❌ 旧版（HTML/CSS 手写、手搭 Relay 节点）规范已废弃，不要参考任何讲 HTML 切图或手写节点骨架的内容。
