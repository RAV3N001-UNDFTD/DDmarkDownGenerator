---
description: JD AI 对话导购助手设计方案生成规范。用户描述场景时自动触发，通过 zero-design MCP 直接写入 Relay。
globs:
  - "schemes/**/*.html"
  - "design/**/*.md"
alwaysApply: true
---

# AI 对话导购助手 · 设计方案生成工作流

## 项目说明

本项目用于快速生成 JD AI 购物助手的回复设计方案。用户描述场景，agent 通过 **zero-design MCP（`use_design_script`）直接写入 Relay**，无需生成 HTML 中间文件。

## 快速开始

用户说「生成一个 XX 场景的方案」时，执行：

1. 确认目标 Relay 节点（用户提供链接，或沿用默认节点）
2. 根据场景选择合适的组合模式（见下方「组合范式」）
3. 通过 `use_design_script` **分批写入 Relay**（每批建议 ≤10 节点操作）
4. 写完后调用 `get_screenshot` 截图确认

> HTML 备份可选：`schemes/[场景名]/scheme-a.html`，**不作为默认输出**。

## 默认 Relay 目标

- **fileKey**：`2059170750769684481` · **pageId**：`0:2`
- 每次脚本开头：`await relay.setCurrentPageAsync(relay.root.children.find(p => p.id === '0:2'))`

## 文件结构

```
对话流markdown/
├── design/ai-reply-design.md    ← 完整设计规范（必读）
├── assets/                      ← SVG 切图资源（HTML 备份用）
└── schemes/[场景名]/scheme-a.html  ← 可选 HTML 备份
```

## 页面基础

- 页面宽度：**375px**，左右 16px 边距，内容有效宽度 **343px**
- body 背景：`#EDEDED`；内容区：`#FFFFFF`，`padding: 16px`
- 字体：`"PingFang SC", -apple-system, "Helvetica Neue", sans-serif`

## Markdown 文本规范

| 类型 | 字号 | 字重 | 行高 | 颜色 |
|------|------|------|------|------|
| H1 | 16px | 600 | 28px | #171A26 |
| H2 | 14px | 600 | 24px | #171A26 |
| 正文 | 14px | 400 | 24px | #3D414D |

## 商品横卡实现规范

### 外层容器
```css
background: #F5F6FA; border-radius: 12px; padding: 8px;
display: flex; gap: 12px; align-items: flex-start;
```

### 图片区（左，86×86）
```css
width: 86px; height: 86px; flex-shrink: 0;
border-radius: 8px; background: #D8D9DE;
position: relative; overflow: hidden;
```

收藏图标叠加（SVG 已含背景，wrapper 只加 blur，不加 background/padding）：
```html
<div class="h-collect">
  <img src="../../assets/收藏.svg" width="16" height="16">
</div>
```
```css
.h-collect {
  position: absolute; top: 4px; left: 4px;
  backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px);
  border-radius: 4px; line-height: 0;
}
```

### 内容区（右，flex:1）
```css
height: 86px; padding-top: 4px;
display: flex; flex-direction: column;
```

#### 上部分：标题

tag `float: left`，商品名单字段自然绕排：
- 行1：文字在 tag 右侧（199px）
- 行2：tag 高 14px，第二行（y≥19px）回到左边，与 tag 左对齐（229px 全宽）✓

```html
<div class="h-top">
  <span class="tag-zying">自营</span>
  <div class="h-name">商品名 规格 副标题（完整文字，自然换行）</div>
</div>
```
```css
/* overflow:hidden 包裹 float + 限高 2 行 */
.h-top { max-height: 38px; overflow: hidden; }

/* 自营 tag — float left，纯 CSS，不用 SVG */
.tag-zying {
  float: left;
  margin-right: 8px;
  margin-top: 3px;
  display: inline-flex; align-items: center;
  height: 14px; padding: 0 3px;
  background: #FF0F23; border-radius: 2px;
  color: white; font-size: 10px; font-weight: 600; line-height: 1;
  white-space: nowrap;
}
/* 商品名：块级，内联内容绕 float，整词换行，line-height 19px 含 5px 行距 */
.h-name {
  font-size: 14px; font-weight: 500; line-height: 19px; color: #171A26;
  word-break: break-word;
}
```

#### 下部分：促销 + 价格（flex-col, gap: 6px）

促销标签**纯 CSS 实现**（不用 SVG 文件）：
```css
.promo-jian {
  display: inline-flex; align-items: center; /* 垂直居中必须 */
  height: 14px; padding: 0 3px; border-radius: 2px;
  outline: 0.33px solid rgba(255,15,35,0.30); outline-offset: -0.33px;
  color: #FF0F23; font-size: 10px; font-weight: 400; line-height: 1;
  white-space: nowrap;
}
.promo-svc {
  display: inline-flex; align-items: center;
  height: 14px; padding: 0 3px; border-radius: 2px;
  outline: 0.33px solid rgba(181,105,26,0.30); outline-offset: -0.33px;
  color: #B5691A; font-size: 10px; font-weight: 400; line-height: 1;
  white-space: nowrap;
}
```

价格行（¥ 与数字 gap: 0）：
```html
<div class="h-price-action">
  <div class="h-price">
    <div class="price">
      <span class="price-sym">¥</span><span class="price-num">890</span>
    </div>
    <span class="sold">已售1万+</span>
  </div>
  <img src="../../assets/加购 & 购买按钮.svg" width="65" height="24">
</div>
```
```css
.h-price-action { display: flex; align-items: center; justify-content: space-between; height: 24px; }
.h-price { display: flex; align-items: baseline; gap: 8px; }
.price { display: flex; align-items: baseline; gap: 0; } /* gap 必须为 0 */
.price-sym { font-size: 12px; font-weight: 600; line-height: 14px; color: #FF0F23; }
.price-num { font-size: 16px; font-weight: 600; line-height: 14px; color: #FF0F23; }
.sold { font-size: 12px; font-weight: 400; line-height: 12px; color: #828794; }
```

## 组合范式

| 模式 | 适用场景 | 结构 |
|------|----------|------|
| A：文本 + 横卡列表 | 多商品快速浏览对比 | `text_block → card×N`，卡间距 10px |
| B：文本 + 三列竖卡 | 多商品快速视觉扫描 | `text_block → vertical_card_3col` |
| C：分段文本交替卡 | 每款有独立推荐理由 | `[H2+body] → card` 循环 |

间距规则：文本块 ↔ 卡片块 **24px**；多横卡之间 **10px**；标题↔promo **6px**（h-bottom margin-top:6px），promo↔价格 **4px**（h-bottom gap:4px）

## Relay 写入规范

### Design Tokens（颜色值 0-1）

| 用途 | Hex | Relay color |
|------|-----|-------------|
| 主文字 | #171A26 | `{r:0.0902,g:0.1020,b:0.1490}` |
| 正文 | #3D414D | `{r:0.2392,g:0.2549,b:0.3020}` |
| 价格/促销红 | #FF0F23 | `{r:1,g:0.059,b:0.137}` |
| 服务标签棕 | #B5691A | `{r:0.710,g:0.412,b:0.102}` |
| 已售灰 | #828794 | `{r:0.510,g:0.529,b:0.580}` |
| 卡片背景 | #F5F6FA | `{r:0.9608,g:0.9647,b:0.9804}` |
| 图片占位 | #D8D9DE | `{r:0.847,g:0.851,b:0.871}` |

### 商品横卡节点结构

```
cardWrap  HORIZONTAL | F5F6FA | r12 | p8 | gap:12
  imgRect   86×86 FIXED | D8D9DE | r8
  bodyFrame FILL×86 FIXED | pt4 | SPACE_BETWEEN
    nameText  14px Medium | FILL | textAutoResize:HEIGHT
    bottomFrame VERTICAL | FILL | HUG | gap:4
      promoRow  HORIZONTAL | HUG | gap:4
        promoTag  HORIZONTAL | p:2 3 | r2 | stroke 0.33px inside 30%
          tagText   10px Regular
      priceAction HORIZONTAL | FILL×24 FIXED | SPACE_BETWEEN
        priceGrp  HORIZONTAL | HUG | gap:8 | BASELINE
          priceText  16px Semibold | #FF0F23
          soldText   12px Regular  | #828794
        buyBtn    65×24 FIXED | r4 | 橙色占位
```

### 关键 API 规则

- **FILL 必须在 `appendChild` 之后设置**
- **`resize()` 会重置为 FIXED**，之后需重新设 `layoutSizingHorizontal = 'FILL'`
- 颜色范围 **0-1**（非 0-255）
- 分批写入，每批建议 **≤10 个节点**

### 与 HTML 版本的已知差异

- 自营标签：Relay 无 float，暂用文字替代
- ¥ 字号：合并为单节点 16px（HTML 版 ¥ 12px + 数字 16px）
- 购买按钮：橙色矩形占位，需设计师替换
- 收藏图标：按需额外脚本添加

## 注意事项（避坑）

- **每次只生成 1 个方案**，确认后再做变体
- 写入完成后必须 `get_screenshot` 截图验证
- 遇节点报错先用 `get_design_metadata` 确认节点 ID
